#!/usr/bin/python3
"""
A beautiful code that passes the pycodestyle checks
"""


def sum_list(lst):
    """Return the sum of values in a list.
    
    It's also an increment
    """
    total = 0
    for val in lst:
        total += val
    return total
